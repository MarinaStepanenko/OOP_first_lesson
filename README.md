# E-commerce Core System

Проект представляет собой ядро для интернет-магазина, реализующее основные сущности электронной коммерции. Система подготавливает фундамент для дальнейшей разработки различных интерфейсов (веб-сайт, телеграм-бот и т.д.).

## Архитектура проекта

### Структура директорий
````
project_root/
├── data/ # Документы
│ ├── products.json
├── src/ # Исходный код приложения
│ ├── init.py # Пакет src
│ ├── base_product.py # Модуль класса BaseProduct
│ ├── category.py # Модуль класса Category
│ ├── main.py # Главный модуль приложения
│ ├── print_mixin.py # Модуль класса PrintMixin
│ ├── product.py # Модуль класса Product
│ ├── product_iterator.py # Итератор для продуктов в категории
│ ├── smartphone.py # Класс Smartphone (наследник Product)
│ └── utils/ # Вспомогательные утилиты
├── htmlcov/ # HTML отчеты о покрытии кода тестами
│ │ ├── index.html # Главная страница отчета
│ │ ├── coverage.css # Стили для отчета
│ │ └── ... # Остальные файлы отчета
├── tests/ # Тесты
│ ├── init.py
│ ├── conftest.py         
│ ├── test_product.py # Тесты для класса Product
│ ├── test_category.py # Тесты для класса Category
│ ├── test_lawngrass.py # Тесты для класса LawnGrass
│ ├── test_print_mixin.py # Тесты для класса LawnGrass
│ └── test_smartphone.py # Тесты для класса Smartphone
├── requirements.txt # Зависимости проекта
├── .gitignore # Исключения для Git
├── pytest.ini # Конфигурация pytest (опционально)
└── README.md # Документация
````

## Описание классов


## Класс `BaseProduct`

Базовый абстрактный класс с именем 
BaseProduct, родительский для класса продуктов.

Классы «Смартфон» и «Трава газонная» остаются наследниками класса 
Product и тем самым наследуют все свойства абстрактного класса.

Реализованы геттеры и сеттеры через фикстуру @property:
- `def price(self):` для вывода приватного атрибута цены
- `def price(self, value):` для установки новой цены
## Класс `Product`

Класс представляет товар в интернет-магазине.

**Атрибуты:**
- `name` (str) - название товара
- `description` (str) - описание товара
- `price` (float) - цена товара
- `quantity` (int) - количество товара в наличии

**Пример использования:**
class Product:
pass

class Product:
pass

```python
product = Product(
    name="LG PC",
    description="Это описание продукта",
    price=156.78,
    quantity=2
)
```
Добавлены магические методы:

```python
    def __str__(self):
        
    def __add__(self, other):
    #Вычисляет общую стоимость товаров 
```

Добавлен  @classmethod для добавления нового товара:
- `def new_product(cls, parameters: dict, product_list: Any | None = None):`
## Класс `Category`

Класс `Category` представляет собой категорию товаров в интернет-магазине. Каждая категория содержит список товаров определенного типа и автоматически участвует в подсчете общей статистики магазина через статические атрибуты класса.

### Структура класса

#### Атрибуты экземпляра
| Атрибут | Тип данных | Описание | Обязательность |
|---------|------------|----------|----------------|
| `name` | `str` | Название категории | Обязательный |
| `description` | `str` | Описание категории | Обязательный |
| `products` | `list[Product]` | Список товаров, принадлежащих категории | Обязательный |

### Атрибуты класса (статические)
| Атрибут | Тип данных | Описание | Поведение при инициализации |
|---------|------------|----------|----------------------------|
| `category_count` | `int` | Общее количество созданных категорий | Увеличивается на 1 при создании новой категории |
| `product_count` | `int` | Общее количество всех товаров во всех категориях | Увеличивается на сумму количеств (`quantity`) всех товаров в новой категории |

## Конструктор `__init__`

### Сигнатура метода
````
def __init__(self, name: str, description: str, products: list) -> None:
````
## Параметры конструктора

### Обязательные параметры

#### 1. `name`
**Тип:** `str`  
**Описание:** Название категории товаров  
**Примеры:**
- `"Смартфоны"`
- `"Ноутбуки"`
- `"Бытовая техника"`

**Требования:**
- Не может быть пустой строкой
- Должен быть читаемым и информативным
- Рекомендуется использовать заглавную букву в начале

#### 2. `description`
**Тип:** `str`  
**Описание:** Подробное описание категории  
**Примеры:**
- `"Мобильные телефоны различных брендов и ценовых категорий"`
- `"Портативные компьютеры для работы и игр"`

**Требования:**
- Может быть пустой строкой (но не рекомендуется)
- Должен описывать общие характеристики товаров категории
- Максимальная длина не ограничена

#### 3. `products`
**Тип:** `list[Product]`  
**Описание:** Список товаров, принадлежащих категории  

**Структура списка:**
class Product:
pass

```python
[   
    Product(name="...", description="...", price=100.0, quantity=5),
    Product(name="...", description="...", price=200.0, quantity=3),
    # ... другие товары
]
```
Для класса Category добавлены магические методы:
- `def __str__(self):` #Для вывода информации по категории в строковом формате

Добавлен геттер с фикстурой @property:
- `def products(self):` #Для вывода списка всех продуктов в строковом формате

## Класс `Smartphone`
Подкласс Product.

**Атрибуты:**
Следующие атрибуты добавлены через конструкт `super.()` в иницилиазации:

- `name` (str) - название товара
- `description` (str) - описание товара
- `price` (float) - цена товара
- `quantity` (int) - количество товара в наличии

Добавлены новые атрибуты `efficiency, model, memory, color` в качестве расширения `__init__`.
Расширен магический метод `__add__` с проверкой типа товара.

## Класс `LawnGrass`
Подкласс Product.

**Атрибуты:**
Следующие атрибуты добавлены через конструкт `super.()` в иницилиазации:

- `name` (str) - название товара
- `description` (str) - описание товара
- `price` (float) - цена товара
- `quantity` (int) - количество товара в наличии

Добавлены новые атрибуты `country, germination_period, color` в качестве расширения `__init__`.
Расширен магический метод `__add__` с проверкой типа товара.

## Класс `PrintMixin`
Класс-миксин, который при создании объекта, то есть при работе метода 
`__init__`, печатает в консоль информацию о том, от какого класса и с какими параметрами был создан объект.

## Тестирование

### Структура тестов

Тесты находятся в директории `tests/`:
````
tests/
├── init.py
├── test_product.py # Тесты для класса Product
├── test_smartphone.py # Тесты для класса Smartphone
├── test_lawngrass.py # Тесты для класса LawnGrass
└── test_category.py # Тесты для класса Category
````
# 📊 Отчет о покрытии тестами

## 📈 Общая статистика

| Метрика | Значение |
|---------|----------|
| **Общее покрытие** | **100%** 🎉 |
| Всего строк кода | 111 |
| Пропущено строк | 0 |
| Исключено строк | 0 |

## 📁 Детализация по файлам

| Файл | Строк | Пропущено | Покрытие |
|------|-------|-----------|----------|
| `src/__init__.py` | 0 | 0 | 100% |
| `src/base_product.py` | 20 | 0 | 100% |
| `src/category.py` | 25 | 0 | 100% |
| `src/lawngrass.py` | 12 | 0 | 100% |
| `src/print_mixin.py` | 6 | 0 | 100% |
| `src/product_iterator.py` | 12 | 0 | 100% |
| `src/product.py` | 23 | 0 | 100% |
| `src/smartphone.py` | 13 | 0 | 100% |

## 🧪 Как запустить тесты

### Базовый запуск
```bash
python -m pytest tests/
```
### Необходимые библиотеки

Для запуска тестов требуется установить:

```bash
pip install pytest
```
## Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.